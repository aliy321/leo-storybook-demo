# Next Steps

## Current Target

1. Button renders in web Storybook (`:6006`).
2. Button renders in RN Storybook (`:6007`).
3. Button styling uses `cva` + `cn` with semantic token classes.
4. Tokens generated from Figma feed both Tailwind and NativeWind.

## Missing

- Visual QA screenshots for both Storybooks.
- Button interaction tests for click/press and disabled state.
- Accessibility checks for labels, focus, and disabled semantics.
- Figma token schema confirmation for v3 semantic roles.
- Token verification that fails when required semantic roles are missing.
- Publish smoke tests for `@leo/button` in web and RN consumers.
