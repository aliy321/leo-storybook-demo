import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import * as {{pascalCase name}}Stories from '../web/{{name}}.stories';

<Meta title="Components/{{pascalCase name}}" of={{{storiesBinding}}} />

# {{pascalCase name}}

{{pascalCase name}} component scaffold — update this overview.

**Supported screens:** Mobile, Tablet, Desktop

## Usage

- Use **default** for the primary surface style.
- Use **outline** when you need a clear boundary without elevation.

## Variants

<table>
  <thead>
    <tr>
      <th>Variant</th>
      <th>Use</th>
    </tr>
  </thead>
  <tbody>
    <tr><td><code>default</code></td><td>Card-like surface with subtle shadow</td></tr>
    <tr><td><code>outline</code></td><td>Bordered surface without shadow</td></tr>
  </tbody>
</table>

## Accessibility

- Provide meaningful slotted content or an accessible name when interactive.
- Disabled state must not trigger actions.

## Implementation

{{pascalCase name}} follows the CVA + semantic token pattern shared with Button and Card. Variants are defined once in `packages/ui/src/{{name}}/core/{{name}}.core.ts` and consumed by web (Stencil) and native (React Native) shells.

## Stories

Use **Default** to explore props interactively. Gallery stories below are visual reference only; their Controls are disabled.

### Default

<Canvas of={ {{storiesName}}.Default } />

### Variants

<Canvas of={ {{storiesName}}.Variants } />

### Disabled

<Canvas of={ {{storiesName}}.Disabled } />

## API

### Web Component (`{{tag name}}`)

<table>
  <thead>
    <tr>
      <th>Property</th>
      <th>Attribute</th>
      <th>Description</th>
      <th>Type</th>
      <th>Default</th>
    </tr>
  </thead>
  <tbody>
    <tr><td><code>variant</code></td><td><code>variant</code></td><td>Surface style</td><td><code>default | outline</code></td><td><code>default</code></td></tr>
    <tr><td><code>disabled</code></td><td><code>disabled</code></td><td>Disabled state</td><td><code>boolean</code></td><td><code>false</code></td></tr>
  </tbody>
</table>

<ArgTypes of={{{storiesBinding}}} />
