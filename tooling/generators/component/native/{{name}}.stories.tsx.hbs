import * as React from 'react';
import { Text, View } from 'react-native';
import type { Meta, StoryObj } from '@storybook/react';
import { fn } from 'storybook/test';
import { {{pascalCase name}} } from './{{name}}.native';
import {
  {{camelCase name}}Contract,
  {{camelCase name}}VariantValues,
  type {{pascalCase name}}Options,
  type {{pascalCase name}}Variant,
} from '@leo/ui/{{name}}';
import { {{constantCase name}}_GALLERY_STORY_PARAMETERS } from '../story-shared';

interface {{pascalCase name}}Args extends {{pascalCase name}}Options {
  variant: {{pascalCase name}}Variant;
  disabled: boolean;
  onPress?: ReturnType<typeof fn>;
}

function StoryRow({ children }: { children: React.ReactNode }) {
  return (
    <View className="flex-row flex-wrap items-start gap-4">
      {children}
    </View>
  );
}

function {{pascalCase name}}Label({ children }: { children: string }) {
  return <Text className="text-foreground">{children}</Text>;
}

const meta: Meta<{{pascalCase name}}Args> = {
  title: 'Components/{{pascalCase name}}',
  component: {{pascalCase name}},
  parameters: {
    layout: 'padded',
    componentContract: {{camelCase name}}Contract,
    docs: { page: () => import('../docs/{{name}}.docs.native.mdx') },
  },
  argTypes: {
    variant: {
      control: 'select',
      options: {{camelCase name}}Contract.props.variant.values,
      description: {{camelCase name}}Contract.props.variant.description,
    },
    disabled: {
      control: 'boolean',
      description: {{camelCase name}}Contract.props.disabled.description,
    },
    onPress: {
      action: 'pressed',
      description: 'Fired when the component is pressed (React Native).',
    },
  },
  args: {
    variant: {{camelCase name}}Contract.props.variant.default,
    disabled: {{camelCase name}}Contract.props.disabled.default,
    onPress: fn(),
  },
};

export default meta;
type Story = StoryObj<{{pascalCase name}}Args>;

export const Default: Story = {
  render: (args) => (
    <{{pascalCase name}} {...args}>
      <{{pascalCase name}}Label>Toggle props in Controls to explore this scaffold.</{{pascalCase name}}Label>
    </{{pascalCase name}}>
  ),
};

export const Variants: Story = {
  parameters: {{constantCase name}}_GALLERY_STORY_PARAMETERS,
  render: () => (
    <StoryRow>
      { {{camelCase name}}VariantValues.map((variant) => (
        <{{pascalCase name}} key={variant} variant={variant}>
          <{{pascalCase name}}Label>{`${variant} variant`}</{{pascalCase name}}Label>
        </{{pascalCase name}}>
      ))}
    </StoryRow>
  ),
};

export const Disabled: Story = {
  parameters: {{constantCase name}}_GALLERY_STORY_PARAMETERS,
  render: () => (
    <StoryRow>
      { {{camelCase name}}VariantValues.map((variant) => (
        <{{pascalCase name}} key={variant} variant={variant} disabled>
          <{{pascalCase name}}Label>{`${variant} (disabled)`}</{{pascalCase name}}Label>
        </{{pascalCase name}}>
      ))}
    </StoryRow>
  ),
};
