import type { {{pascalCase name}}Variant } from './{{name}}.core';

export interface {{pascalCase name}}Options {
  variant?: {{pascalCase name}}Variant;
  disabled?: boolean;
  className?: string;
}

export type { {{pascalCase name}}Variant };
